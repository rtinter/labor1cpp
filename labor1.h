double squared(double b);

void passByReference(int &a);

void passByPointer (int *ptr);

using Person = struct{
    std::string firstName;
    std::string lastName;
    std::string birthday;
    int ageInYears;
    int ageInDays;
    int ageInHours;
};


